services:
  - type: web
    name: ibuscribe-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ibuscribe-db
          property: connectionString
      - key: GROQ_API_KEY
        sync: false
      - key: ABDM_CLIENT_ID
        sync: false
      - key: ABDM_CLIENT_SECRET
        sync: false
      - key: ABDM_GATEWAY_URL
        value: https://dev.abdm.gov.in
      - key: SECRET_KEY
        generateValue: true

databases:
  - name: ibuscribe-db
    databaseName: ibuscribe
    plan: free
